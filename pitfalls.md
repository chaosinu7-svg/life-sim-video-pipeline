# 已实证的踩坑与规避

下面每一条都来自 2026-08-10 EP01 的**真实事故**，不是推演。
共同特征：**渲染成功、脚本零报错、自动质检全绿，但成片是坏的。**

---

## 一、开头视频禁止用 `-c copy` 拼接

**症状**：成片里标题卡整段消失，从快闪直接跳进正文。播放器播那个开头视频完全正常，容器时长也对。

**根因**：三段 `-c copy` 拼出来的文件，每段保留各自的帧编号。下游
`render_streaming_ffmpeg.mjs` 用 `trim=start_frame=0:end_frame=N` 截取时，
数到第二段末尾就停。**实测请求 199 帧只截出 110 帧。**

**规避**：拼接必须重编码。

```bash
ffmpeg -f concat -safe 0 -i list.txt -vsync cfr -r 30 \
       -c:v libx264 -pix_fmt yuv420p -crf 16 out.mp4
```

**自检**：拼完立刻验一次 trim 能不能截满。

```bash
ffmpeg -i opening.mp4 -filter_complex "[0:v]trim=start_frame=0:end_frame=199[v]" \
       -map "[v]" -f null - 2>&1 | grep frame=
```

---

## 二、生图侧不许碰几何

**症状**：近半数图带纯黑立柱边。带 zoom/pan 一动，黑边跟着漂。

**根因**：`image_gen` 交付的像素尺寸不等于请求尺寸——实测出过
1448×1086(4:3)、1672×941、810×1080(竖)。让生图 agent 自己"凑到 1920×1080"，
它会等比缩放加黑边。**明确写"禁止补边"之后它仍然照做了两轮。**

**规避**：分工写死——**codex 只出原图并原样保存，几何由 `scripts/normalize_scenes.py` 统一处理**。
任务书里必须有这句：

> 只负责生成并保存到 output 路径，禁止任何缩放、裁切、补黑边、加白边、改尺寸的后处理。
> 如果交付的宽/高 < 1.5，重新生成，不要靠补边凑。

**归一器的两个坑**（都修过）：黑边检测只认精确为 0 的纯黑且要求左右/上下对称，
否则暗夜镜头会被误判；内容比 16:9 更宽时要裁宽度而不是高度，否则算出负坐标。

---

## 三、"禁止一切文字"和"画面主体是数字"会打架

**症状一**：底稿特写上出现「作者：林砚」——**剧本里根本不存在的人名**。
**症状二**：评议表画成星级评分和 95/90/85，旁白说的是 40%/30%；计算器显示 987.50，旁白说的是四千五。

**根因**：中文汉字模型写不对，必须全禁；但有些镜头的主体就是那个数字，
全禁会让镜头失去意义，模型于是自由发挥。

**规避**：分两类。**汉字一律禁止**；**描述里含具体数字的镜头单独放开阿拉伯数字**，
并在提示词里写死具体数值。`build_storyboard.py` 会按关键词自动分类，但**漏一个那镜就废**，
生成前把白名单打出来人工过一遍。

需要"有文字感但不可读"的镜头（聊天气泡、公示名单），写：

> 文字必须**不可读**：用失焦、虚化、极浅景深或抽象笔画质感表现，
> 让观众一眼看出那是文字、但认不出任何一个字。

---

## 四、手机正反面

**症状**：屏幕朝着镜头显示时间，而人物的视线也朝着手机——**等于他在看背面**。
摄像头凸起还画在了同一面。

**规避**：凡是"人物看手机"的镜头，**一律拍背面**：

> 镜头在人物侧后方或过肩，只看得到手机的背面，屏幕朝着人物自己，
> 屏幕的冷光从边缘漏出来打在他脸上。**画面里不出现任何屏幕内容。**

屏幕内容交给旁白说。纯界面特写（无人）才允许显示内容。

---

## 五、章节标题别自带「第X幕」

渲染器会自动加 `第${中文数字}幕` 前缀。`PROJECT_SPEC.source.chapters[].title`
里再写一遍，成片上就是「第一幕 第一幕 我们组这次是这么想的」。

---

## 六、`codex exec` 后台跑必须加 `< /dev/null`

否则会挂在 `Reading additional input from stdin...`，**一张图不出、也不报错**，
你以为它在跑，其实它在等输入。

大批量生图还会撞 `Selected model is at capacity`。任务书里写：**等 30 秒重试，最多 5 次**，
否则它会直接放弃剩下的全部任务。

---

## 七、`silencedetect` 会误判脉冲音效

棘轮/齿轮这类稀疏脉冲音，咔哒之间的间隙低于阈值，会被判成大段静音。
**别直接信报告**——逐 0.5 秒量峰值复核：

```bash
for i in $(seq 0 0.5 10); do
  printf "%ss " $i
  ffmpeg -nostats -ss $i -t 0.5 -i final.mp4 -af volumedetect -f null - 2>&1 | grep max_volume
done
```

EP01 那次报的"2.665–3.849 静音"，实测该区间峰值在 −4.4 到 −8.2 dB，音效完整存在。

---

## 八、改文案会让所有图配错画面

镜头靠 cue（正文原句片段）锚定。文案一改，节拍重排、编号平移，
**已有的图全部对应到错误的镜头，而渲染不报错、成片能正常播放**。

改完文案必须跑 `scripts/remap_scenes.py`，用文本相似度把旧图搬到新编号，
匹配不上的重生成。EP01 改稿后 155 镜里 150 张可继承、5 张要新画。

## 九、image_gen 对年轻女性角色有系统性美型化倾向

**2026-08-11 EP02 实证。** 同一批提示词、同一段画风描述，四张锚点出来分成两组：

| 角色 | 结果 |
|---|---|
| 46岁男主任 | ✅ 写实厚涂，皮肤有质感，法令纹眼袋都画出来 |
| 38岁女会计 | ✅ 同上 |
| 23岁女应届生 | ❌ 日系美型：平涂、下巴尖、鼻小眼大、网红脸 |
| 22岁女前台 | ❌ 同上，且**收腰制服 + 勾出胸线 + 对镜头甜笑** |

中年角色老老实实写实，年轻女性自动往二次元美少女跑。
EP01 全是男性角色，所以这条一直没暴露。

**为什么这不只是画风问题。** EP02 的立意是"问题在看的人身上，不在穿的人身上"。
前台那张图自己在替加害者的视线说话——镜头把她当成了观赏对象。
一旦观众心里浮起一丝「她确实是打扮了」，恨意就散了，整条片子废掉。

**修法（已验证有效）**
1. 让 codex **先打开已通过的中年角色锚点看一眼**，再画年轻角色。文字描述压不住这个倾向，图能。
2. 提示词里**点名上一版的失败**：「不是日系美少女、不是二次元、不是网红脸」。
3. 写死解剖学约束：脸型不许过分尖削、下巴不许过尖、眼睛不大、鼻子嘴唇比例写实、皮肤要有质感不许平涂。
4. 写死身体与服装约束：**不摆拍、不对镜头微笑、不强调身材曲线、版型宽松平直、不收腰、不贴身**。
5. 角色不需要好看，需要**普通**。

## 十、别把 `narration-full.vtt` 当字幕看，也别把 18 字当溢出线

**2026-08-11 EP02，同一个节点上连着误判两次，两次都是虚警。**

**误判一**：拿 `narration-full.vtt` 量字幕长度，发现 193 段里 72 段超 18 字、最长 42 字，
以为字幕会溢出。**错了**——那份 VTT 是**句级的主时间轴**，不是字幕。
真正的字幕切分在 `build_narration.mjs` 的 `buildCaptionCues()`，
结果写进 `audio_meta.json` 和 `STORYBOARD.json`。要验字幕只能验这两个文件。

**误判二**：`audio_meta.json` 里确实有 11 条超 18 字（最长 21），又以为要修。
**算一下就知道不用**：`HBG_STYLE.captions` 里字号 48、字距 0.035em → 每字约 49.7px，
`maxWidth` 1540px → **单行放得下 31 字**。21 字才 1043px，余量三分之一。
`captionMaxChars: 18` 是**风格设定**（短字幕读得快），不是物理上限，超一点无害。

**还有一条顺序问题**：`build_narration.mjs` 要读 `STORYBOARD_BASE.json`，
而那是第 6 步的产物。所以第一次跑它**一定会在末尾 ENOENT 崩掉**，
但音频（含 VTT）已经正常产出了。正确顺序是
**跑一次 build_narration 拿音频和 VTT → 排分镜出 STORYBOARD_BASE → 再跑一次 build_narration 补齐字幕**。
第一次那个崩溃是预期行为，不要去"修"。
